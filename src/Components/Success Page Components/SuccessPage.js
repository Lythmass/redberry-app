import React from 'react'
import styled from 'styled-components'

export default function SuccessPage() {

     React.useEffect(() => {
          localStorage.clear();
     }, []);
     
     return (
          <section>
               hwef
          </section>
     )
}
